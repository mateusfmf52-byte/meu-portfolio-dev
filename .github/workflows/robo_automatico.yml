name: Robô Curador de Wallpapers

on:
  schedule:
    - cron: '0 */6 * * *' # Roda a cada 6 horas
  workflow_dispatch: # Permite rodar manualmente clicando num botão

permissions:
  contents: write

jobs:
  baixar-e-atualizar:
    runs-on: ubuntu-latest

    steps:
    - name: Baixar repositório (Checkout)
      uses: actions/checkout@v3

    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Instalar bibliotecas
      run: |
        pip install requests

    - name: Rodar o Robô
      run: python bot_wallpapers.py

    - name: Salvar e Enviar mudanças (Commit & Push)
      run: |
        git config --global user.name "Robô GitHub"
        git config --global user.email "robo@github.com"
        git add .
        git commit -m "🤖 Nova imagem adicionada via Automação" || echo "Nada para commitar"
        git push